var mimeType = requestResponse.mimeType();

return mimeType != MimeType.CSS
 && mimeType != MimeType.IMAGE_UNKNOWN
 && mimeType != MimeType.IMAGE_JPEG
 && mimeType != MimeType.IMAGE_GIF
 && mimeType != MimeType.IMAGE_PNG
 && mimeType != MimeType.IMAGE_BMP
 && mimeType != MimeType.IMAGE_TIFF
 && mimeType != MimeType.UNRECOGNIZED
 && mimeType != MimeType.SOUND
 && mimeType != MimeType.VIDEO
 && mimeType != MimeType.FONT_WOFF
 && mimeType != MimeType.FONT_WOFF2
 && mimeType != MimeType.APPLICATION_UNKNOWN
 /*在这下面添加规则*/
 && !requestResponse.contains("googleapis.com", false)
 && !requestResponse.contains("google.com", false)
 && !requestResponse.contains("firefox.com", false)
 && !requestResponse.contains("mozilla.org", false)
 && !requestResponse.contains("baidu.com", false)
 && !requestResponse.contains("gtimg.com", false)
 && !requestResponse.contains("github.com", false)
 && !requestResponse.contains("csdn.net", false)
 && !requestResponse.contains("aliyun.com", false)
 && !requestResponse.contains("adtidy.org", false)
 && !requestResponse.contains("qianxin.com", false)
 && !requestResponse.contains("immersivetranslate.com", false)
 && !requestResponse.contains("mozilla.com", false)
 && !requestResponse.contains("openjfx.cn", false)
 && !requestResponse.contains("feishu.cn", false)
 && !requestResponse.contains("grok.com", false)
 && !requestResponse.contains("map.qq.com", false)
 && !requestResponse.contains("mozilla.net", false)
 && !requestResponse.contains("qpic.cn", false)
 && !requestResponse.contains("amazonaws.com", false)
 && !requestResponse.contains("gstatic.com", false)
 && !requestResponse.contains("aliapp.org", false)
 && !requestResponse.contains("alicdn.com", false)
 && !requestResponse.contains("greasyfork.org", false)
 && !requestResponse.contains("sohu.com", false);

